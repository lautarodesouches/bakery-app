import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 0.85,
        alignItems: 'center',
    },
    cartList: {
        marginTop: 5,
        width: '100%'
    },
    text: {
        fontSize: 20,
    }
})